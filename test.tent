name = "Version";
version_num = 1;
version_dec = 0.5;
total_version = version_num + version_dec;
print(name);

total_version = total_version + 2;
print(total_version);

if total_version == 3.5 {
	total_version = 4;
	print("New integer version:");
	print(total_version);
}