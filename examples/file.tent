load "io";

class File(filename) {
	fi = io.file__openFile(filename);

	form isOpen() {
		return fi != -1;
	}

	form readLine() {
		return io.file__readLine(fi);
	}

	form readFile() {
		return io.file__readFile(fi);
	}

	form close() {
		io.file__closeFile(fi);
		fi = -1;
	}
}
